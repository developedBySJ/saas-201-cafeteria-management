<div class="px-1 overflow-hidden max-w-screen-lg mx-auto px-8 pt-16">
  <div class="pb-6 border-b border-green-200">
    <h1 class="text-green-900 text-5xl font-semibold">Today's Menu</h1>
  </div>
  <div class="flex flex-wrap -mx-3 mt-3">
  <% menu_items.each do |menu_item| %>
  <div class="my-3 px-2 w-full overflow-hidden sm:w-full md:w-1/2 lg:w-1/3 xl:w-1/3">
    <%= render partial: "menu_item_card", locals: { menu_item: menu_item } %>
  </div>
  <% end %>

</div>

</div>